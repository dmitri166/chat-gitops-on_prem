replicaCount: 1

image:
  repository: localhost:5002/chat-app
  tag: latest
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "300m"
    memory: "256Mi"

service:
  type: ClusterIP
  port: 80

nodeSelector: {}
tolerations: []
