replicaCount: 2

nodeSelector:
  env: nonprod

tolerations:
  - key: env
    operator: Equal
    value: nonprod
    effect: NoSchedule

image:
  repository: k3d-k3d-registry:5000/chat-app
  tag: "1.0.0_dev"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

logs:
  emptyDir: {}
  pvc: ""

terraformManagedService: false